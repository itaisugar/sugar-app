{
  "buildCommand": "npx expo export --platform web && node scripts/inject-pwa-head.js",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
